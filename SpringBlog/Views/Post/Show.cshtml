@model Post
@{
    ViewBag.Title = "Show";
}

<h1 class="mt-1">@Model.Title</h1>

<div class="show-post">
    @if (!string.IsNullOrEmpty(Model.PhotoPath))
    {
        <img src="@Url.FeaturedImage(Model.PhotoPath)" class="mb-3" alt="Post Featured Image" />
    }
    @Html.Raw(Model.Content)
</div>